version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: docker/prod.Dockerfile
    ports:
      - "8001:80"
    links:
      - php
    restart: always
    depends_on:
      - php
  php:
    image: php:7-fpm
    restart: always